# 빌드 산출물 — 루트 앵커가 없으면 cmd/backtest/ 같은 디렉터리까지 무시된다
/gld91
/train
/backtest
/probe
/align
/goldencheck

# 시크릿
.env
*.env

# 런타임 산출물
.omc/
.superpowers/
data/
cache/
out/

*.log
# 게이트 결과는 기록으로 남긴다
!docs/results/*.log
/models.json

# Task 9 가 tools/ 에서 npm install 을 돌린다. 앵커 없이 적어 어느 깊이에서든
# 무시한다 — package.json 과 package-lock.json 은 커밋한다(골든 벡터를 다시
# 만들 때 같은 버전이 나와야 한다).
node_modules/

# 빌드 산출물. `make monitor` 는 bin/ 에, 손으로 부른 go build 는 저장소 루트에
# 떨군다. 커밋되면 아키텍처가 다른 기계에서 조용히 옛 바이너리가 돈다.
bin/
/gld91-monitor
